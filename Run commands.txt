# 1. Install dependencies
pip install -r requirements.txt

# 2. Set up your credentials
cp .env.example .env
# → Edit .env with your endpoint URL, API key, model name

# 3. Run
streamlit run app.py