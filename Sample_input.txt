import os
import requests
import json
import sys

DB_PASSWORD = "supersecret123"
API_KEY = "sk-abc123xyz"

def fetch_user_data(user_id):
    url = "https://api.example.com/users/" + user_id
    response = requests.get(url)
    data = response.json()
    return data

def calculate_discount(price, discount):
    result = price - (price * discount / 100)
    return result

def save_to_db(data):
    try:
        print("Saving:", data)
    except:
        pass

def process_all_users(users):
    results = []
    for u in users:
        d = fetch_user_data(u)
        results.append(d)
    return results
```

---

### ✅ Checklist — paste this into the **"Quality Checklist"** box:
```
1. Functions have docstrings
2. No hardcoded credentials or secrets
3. Proper error handling (no bare except)
4. Input validation is present
5. Code follows PEP8 naming conventions
6. No unused imports
7. API responses are checked for errors
8. Code is readable and well-structured
